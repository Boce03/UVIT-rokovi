<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" type="text/css" href="/1.css">
</head>
<body>
    <form id="forma" method="POST" action="/userList/starost">
        <table>
            <tr>
                <td><input type="number" value="21" name="starost"></td>
                <td><input type="submit" value="Filtriraj"></td>
            </tr>
        </table>
    </form>
    <% if(korisnici.length > 0) {%>
        <h1 id="naslov">LISTA KORISNIKA</h1>
        <table id="tabela">
            <tr>
                <th>SLIKA</th>
                <th>INFORMACIJE</th>
            </tr>

            <% for(let korisnik of korisnici) {%>
                <tr>
                    <td rowspan="3" class="slika donja"><img src="/user.png", alt="korisnik" width="100" height="100"></td>
                    <td>IME: <%= korisnik.ime %></td>
                </tr>
                <tr>
                    <td>STAROST: <%= korisnik.starost %></td>
                </tr>
                <tr>
                    <td class="donja">
                        <form class="forma" method="GET" action='/userList/user'>
                            <input type="text" name="id" value=<%= korisnik._id %> hidden>
                            <% if(korisnik.admin) {%>
                                <input type="submit" value="OBRISI KORISNIKA" class="dugme" disabled>
                            <% } else {%>
                                <input type="submit" value="OBRISI KORISNIKA" class="dugme">
                            <% } %>
                        </form>
                    </td>
                </tr>
            <% } %>
        </table>
    <% } else {%>
        <h1>Nema korisnika!</h1>
    <% } %>
</body>
</html>