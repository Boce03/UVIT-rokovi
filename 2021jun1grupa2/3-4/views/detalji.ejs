<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <% if(rez.length > 0) {%>
        <h1>Detaljnije informacije za grad <%= grad %></h1>
        <ul>
            <% for(let r of rez) {%>
                <li>
                    Dana <%= r.datum.getDate() + '.' + (r.datum.getMonth() + 1) + '.' + r.datum.getFullYear() + '.' %>
                    izmerena je temperatura od <%= r.temperatura %> Celzijusa
                </li>
            <% } %>
        </ul>
    <% }else { %>
        <h1>Nema zabelezenih merenja!</h1>
    <% } %>
</body>
</html>